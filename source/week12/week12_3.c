#include <stdio.h>
#define AL 5

int main(){

    int s_list[AL] = {5, 4, 3, 2, 1};
    int a, b, temp, sort;

    printf("초기> 배열 요소: 5 4 3 2 \n");
    printf("\n[for 문으로 선택 정렬 수행]\n");

    for(a=0; a<AL-1; a++){
        sort = a;
    }

    // 선택정렬 도중

    return 0;
}